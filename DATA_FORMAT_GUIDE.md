# دليل تنسيق الملفات المدعومة

## ✅ الملفات المدعومة

يمكنك رفع الملفات التالية في الويب:

1. **ملفات CSV** (`.csv`)
   - ملفات Excel المحفوظة كـ CSV
   - ملفات نصية بفاصلة

2. **ملفات Excel** (`.xlsx`, `.xls`)
   - Microsoft Excel 2007 وما فوق (.xlsx)
   - Excel القديم (.xls)

## 📋 تنسيق الملف المطلوب

### البنية الأساسية:

```
| Feature1 | Feature2 | Feature3 | ... | Target |
|----------|----------|----------|-----|--------|
|   1.2    |   3.4    |   5.6    | ... |   0    |
|   2.3    |   4.5    |   6.7    | ... |   1    |
|   ...    |   ...    |   ...    | ... |  ...   |
```

### المتطلبات:

1. **الصف الأول**: يجب أن يحتوي على أسماء الأعمدة (Headers)
2. **الأعمدة**: كل عمود يمثل ميزة (Feature)
3. **عمود الهدف**: يجب أن يكون موجوداً (عادة آخر عمود أو يمكن اختياره)
4. **لا توجد قيم مفقودة**: يُفضل تنظيف البيانات مسبقاً (البرنامج يحاول معالجتها تلقائياً)

### مثال CSV:

```csv
age,income,education_years,target
25,50000,12,0
30,75000,16,1
35,100000,18,1
```

### مثال Excel:
نفس البنية، لكن في ملف Excel

## 📊 نصائح للملفات

### ✅ ما يجب فعله:
- تأكد من وجود عناوين الأعمدة في الصف الأول
- استخدم أسماء واضحة للأعمدة
- تأكد من أن البيانات رقمية (أو قابلة للتحويل)
- عمود الهدف يجب أن يكون رقمي (0, 1 للتصنيف)

### ❌ ما يجب تجنبه:
- صفوف فارغة تماماً
- أعمدة فارغة تماماً
- أسماء أعمدة متكررة
- بيانات غير رقمية في الأعمدة العادية

## 🔍 التحقق من الملف

قبل الرفع، تأكد:
- حجم الملف أقل من 16 MB
- الملف بصيغة `.csv` أو `.xlsx` أو `.xls`
- البيانات منظمة بشكل جدول
- عمود الهدف موجود

## 📝 أمثلة على ملفات تجريبية

إذا كنت تريد تجربة، يمكنك استخدام:

1. **بيانات بسيطة**: أنشئ ملف CSV به 10-50 ميزة و 100-1000 عينة
2. **بيانات حقيقية**: أي dataset من Kaggle أو UCI Machine Learning Repository

### مثال لإنشاء ملف تجريبي:

```python
import pandas as pd
import numpy as np

# إنشاء بيانات تجريبية
data = {
    'feature1': np.random.randn(100),
    'feature2': np.random.randn(100),
    'feature3': np.random.randn(100),
    'target': np.random.randint(0, 2, 100)
}

df = pd.DataFrame(data)
df.to_csv('my_dataset.csv', index=False)
```

## 🚀 طريقة الرفع

1. افتح الويب: http://localhost:5000
2. اسحب الملف إلى منطقة الرفع أو انقر للتصفح
3. اختر عمود الهدف (أو اتركه على الافتراضي)
4. اضغط "تشغيل التحليل"
5. انتظر النتائج (قد يستغرق دقيقة أو أكثر حسب حجم البيانات)

## ⚠️ ملاحظات مهمة

- **حجم البيانات**: كلما زاد حجم البيانات، زاد وقت المعالجة
- **عدد الميزات**: البرنامج يعمل بشكل أفضل مع 10-1000 ميزة
- **عدد العينات**: يُفضل 100 عينة على الأقل
- **الهدف**: يجب أن يكون تصنيف (Classification) - قيم منفصلة (0, 1, 2, ...)

---

**جاهز للرفع!** 🎉

